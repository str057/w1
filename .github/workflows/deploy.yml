name: Django CI/CD Pipeline

on:
  push:
    branches: [dev]  # ← измени на dev
  pull_request:
    branches: [dev]  # ← измени на dev

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:13
        # ... остальное без изменений

    steps:
    # ... шаги без    изменений

  deploy-notification:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/dev'  # ← dev